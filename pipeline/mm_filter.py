"""Market-maker filter — the make-or-break step (Phase 1, Step 1.5).

Three-signal classifier (inventory flatness, cross-market breadth, native role) with the
decision rule and validation diagnostics (MM gross-vs-net flow shares, flatness bimodality,
top-removed-name spot-check) defined in FALSIFICATION.md.

Built incrementally. Nothing here runs yet.
"""
